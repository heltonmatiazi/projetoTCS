<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="senac.com.br.cademeulivro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity"
            >

        </activity>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".activities.ObraDetalhadaActivity" />
        <activity android:name=".activities.edit.ObraDetalhadaEditActivity"></activity>
        <activity android:name=".activities.edit.ContainerEditActivity"></activity>
        <activity android:name=".activities.RecomendarObra"></activity>
    </application>

</manifest>